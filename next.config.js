/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // basePath: '/blog',
    // assetPrefix: './',
    output: 'export',
  }
  
module.exports = nextConfig